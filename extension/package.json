{
  "name": "schweizermakler-extension",
  "version": "1.0.0",
  "description": "Preisänderungen bei Immobilienportalen im Blick behalten + Standort-Score",
  "private": true,
  "scripts": {
    "icons": "node scripts/generate-icons.js",
    "dev": "npm run icons && webpack --watch --mode development",
    "build": "npm run icons && webpack --mode production",
    "build:firefox": "npm run icons && webpack --mode production --env target=firefox",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "copy-webpack-plugin": "^12.0.2",
    "rimraf": "^6.0.1",
    "sharp": "^0.34.5",
    "ts-loader": "^9.5.1",
    "typescript": "^5.7.3",
    "webpack": "^5.97.1",
    "webpack-cli": "^6.0.1"
  }
}
