/**
 * Content Script für immowelt.de — STUB
 *
 * Phase 4: Wird später implementiert.
 * Risikobewertung: MITTEL — § 9.3 AGB: "jenseits privater Benutzung" verboten.
 *
 * Hinweis: Immowelt lädt vieles per JavaScript nach.
 * MutationObserver NUR zum Lesen verwenden, NICHT zum Schreiben.
 */

import { waitForIdle } from "./base-parser";

waitForIdle(() => {
  // Stub — Implementierung folgt in Phase 4
});
